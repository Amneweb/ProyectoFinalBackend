{{> nav}}
<section class="main">
  <h1>Hola, bienvenidos al panel de administración de Windward Car &amp; Truck -
    Battery Store</h1>
  <h2>Listado de productos en stock</h2>
  <ul class="principal">
    <li>
      <ul class="interna encabezado">
        <li>Nombre producto</li><li>ID</li><li>Precio</li><li
        >Descripción</li><li>SKU</li>
        <li>Stock</li><li>IMG</li><li>Borrar</li>
      </ul>
    </li>

    {{#each productosObtenidos.docs}}
      <li id="li_{{this._id}}">
        <ul class="interna">

          <li><a
              href="/admin/producto/{{this._id}}"
              id="{{this._id}}"
              class="boton-nombre"
              name="{{this.title}}"
            >{{this.title}}</a></li>
          <li> {{this._id}}</li>
          <li> {{formatear this.price}}</li>
          <li>{{this.description}} </li>
          <li>{{this.code}}</li>
          <li>{{this.stock}}</li>
          <li>{{#if this.thumb}}
              <img src="/uploads/img/products/{{this.thumb}}" alt="{{this.title}}" />{{/if}}
          </li><li><button
              id="{{this._id}}"
              class="borrarProducto"
            >🗑️</button></li>
        </ul>
      </li>
    {{/each}}
  </ul>
</section>

<section>
  <h2>Agregar productos</h2>
  <p>NOTA: La imagen del producto se puede cargar una vez guardado el producto, desde el formulario de modificación de producto, haciendo click en el botón gris del producto elegido en el listado de arriba.</p>
  <form
    id="agregar"
    action=""
    enctype="multipart/form-data"

  >
    <label for="title">Title</label>
    <input type="text" id="title" name="title" value="" required />
    <label for="price">Price</label>
    <input type="number" id="price" name="price" value="" required />
    <label for="category">Category</label>
    <select name="category" id="category">
      <option value="" required>Elegir categoría</option>
      {{#each categoriasExistentes}}
        <option value={{this.category}}>{{this.category}} </option>
      {{/each}}
    </select>
    <small></small><small>Si querés agregar una nueva categoría, utilizá el
      formulario de más abajo.</small>
    <label for="code">Code</label>
    <input type="text" id="code" name="code" value="" required />
    <label for="description">Descripcion</label>
    <textarea
      type="text"
      id="description"
      value=""
      name="description" required
    ></textarea>
    <label for="stock">Stock</label>
    <input type="number" id="stock" value="" required name="stock" />
    <label for="status">Status</label>
    <input type="text" value="true" />

    <input type="submit" value="enviar" />
  </form>
  <div class="errores" id="errorAgregar"></div>
  <h2>Administrar categorías</h2>
  <form action="" id="agregarCategorias">
    <label for="newcate">Ingresá una nueva categoría
      <input type="text" id="newcate" name="newcate" />
    </label>
    <button type="submit">Agregar</button>
  </form>
</section>
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script src="/js/catalogoAdmin.js"></script>