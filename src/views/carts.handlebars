<div class="container">

  <h1>Hola, bienvenidos a la administración de tu tienda online</h1>
  <h2>Listado de carritos de compra</h2>
  <ul class="principal">

    {{#each carritosObtenidos}}
      <li>
        <ul class="interna-carts sub-encabezado">
          <li>ID carrito</li>
          <li>{{this._id}}
            <button class="borrarCarrito" id="{{this._id}}">❌</button>
          </li>
        </ul>
      </li>
      <li>
        <ul class="interna-carts encabezado">
          <li>Cantidad</li><li>Nombre producto</li>
        </ul></li>
      {{#each this.cart}}

        <li>
          <ul class="interna-carts">
            <li> {{this.qty}}</li>
            <li>{{this.product.title}}
              <button
                id="{{this.product._id}}"
                class="borrar"
                name="{{../this._id}}"
              >🗑️</button>
            </li>

          </ul>
        </li>
      {{/each}}

    {{/each}}

  </ul>
</div>
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script src="/js/carts.js"></script>